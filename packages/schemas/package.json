{
  "private": true,
  "name": "@clipboard-sync/schemas",
  "type": "module",
  "exports": {
    ".": "./dist/index.mjs",
    "./package.json": "./package.json"
  },
  "types": "./dist/index.d.mts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsdown --watch",
    "build": "tsdown",
    "lint": "eslint . && prettier --check .",
    "lint:fix": "eslint . --fix && prettier --write .",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^4.4.1"
  },
  "devDependencies": {
    "@clipboard-sync/eslint-config": "workspace:*",
    "eslint": "^9.39.4",
    "prettier": "^3.8.3",
    "tsdown": "^0.20.3",
    "typescript": "^5.9.3"
  }
}
